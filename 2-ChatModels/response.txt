content='A prime number is a natural number greater than 1 that has no positive divisors other than 1 and itself.\n\nHere\'s a Java program that checks if a given number is prime, incorporating common optimizations:\n\n```java\nimport java.util.Scanner; // Required for user input\n\npublic class PrimeChecker {\n\n    /**\n     * Checks if a given integer is a prime number.\n     *\n     * @param number The integer to check.\n     * @return true if the number is prime, false otherwise.\n     */\n    public static boolean isPrime(int number) {\n        // 1. Handle base cases:\n        // Numbers less than or equal to 1 are not prime.\n        if (number <= 1) {\n            return false;\n        }\n        // 2 is the only even prime number.\n        if (number == 2) {\n            return true;\n        }\n        // All other even numbers are not prime.\n        if (number % 2 == 0) {\n            return false;\n        }\n\n        // 2. Check for odd divisors from 3 up to the square root of the number.\n        // We only need to check up to the square root because if a number \'n\' has a divisor \'d\'\n        // greater than sqrt(n), then \'n/d\' must be a divisor less than sqrt(n).\n        // We increment by 2 (i += 2) to only check odd numbers, as even numbers were already handled.\n        for (int i = 3; i * i <= number; i += 2) {\n            if (number % i == 0) {\n                return false; // Found a divisor, so it\'s not prime\n            }\n        }\n\n        // If no divisors were found, the number is prime\n        return true;\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in); // Create a Scanner object to read user input\n\n        System.out.print("Enter a number to check if it\'s prime: ");\n        int num = scanner.nextInt(); // Read the integer input from the user\n\n        if (isPrime(num)) {\n            System.out.println(num + " is a prime number.");\n        } else {\n            System.out.println(num + " is not a prime number.");\n        }\n\n        scanner.close(); // Close the scanner to release system resources\n    }\n}\n```\n\n### How the Code Works:\n\n1.  **`import java.util.Scanner;`**: This line imports the `Scanner` class, which is used to get input from the user.\n\n2.  **`public class PrimeChecker { ... }`**: This defines a class named `PrimeChecker` which will contain our logic.\n\n3.  **`public static boolean isPrime(int number) { ... }`**:\n    *   This is a static method that takes an integer `number` as input and returns `true` if it\'s prime, `false` otherwise.\n    *   **Base Cases:**\n        *   `if (number <= 1)`: Numbers less than or equal to 1 are *not* prime by definition.\n        *   `if (number == 2)`: The number 2 is the smallest and only even prime number.\n        *   `if (number % 2 == 0)`: If the number is greater than 2 and is even, it\'s divisible by 2, so it cannot be prime.\n    *   **Optimization (Looping):**\n        *   `for (int i = 3; i * i <= number; i += 2)`: This loop checks for divisors.\n            *   It starts `i` from 3 (since we\'ve already handled 2 and even numbers).\n            *   `i * i <= number`: This is an optimized condition. Instead of checking up to `number / 2` or `Math.sqrt(number)`, we check `i * i <= number`. If `number` has a divisor `d`, then `number = d * k`. If `d > sqrt(number)`, then `k` must be less than `sqrt(number)`. So, we only need to check divisors up to the square root of the number. This significantly reduces the number of iterations for large numbers.\n            *   `i += 2`: We only check odd numbers as potential divisors because we\'ve already eliminated even numbers.\n        *   `if (number % i == 0)`: If `number` is perfectly divisible by `i` (meaning there\'s no remainder), then `i` is a divisor other than 1 and itself, so the number is not prime. We immediately return `false`.\n    *   **Return `true`**: If the loop completes without finding any divisors, it means the number is prime.\n\n4.  **`public static void main(String[] args) { ... }`**:\n    *   This is the main method where the program execution begins.\n    *   `Scanner scanner = new Scanner(System.in);`: Creates a `Scanner` object to read input from the console.\n    *   `System.out.print("Enter a number to check if it\'s prime: ");`: Prompts the user to enter a number.\n    *   `int num = scanner.nextInt();`: Reads the integer entered by the user.\n    *   `if (isPrime(num)) { ... } else { ... }`: Calls the `isPrime` method with the user\'s number and prints the appropriate message based on the returned boolean value.\n    *   `scanner.close();`: It\'s good practice to close the `Scanner` object when you\'re done with it to prevent resource leaks.\n\n### How to Compile and Run:\n\n1.  **Save:** Save the code as `PrimeChecker.java`.\n2.  **Compile:** Open a terminal or command prompt, navigate to the directory where you saved the file, and compile it using the Java compiler:\n    ```bash\n    javac PrimeChecker.java\n    ```\n3.  **Run:** After successful compilation, run the program:\n    ```bash\n    java PrimeChecker\n    ```\n    The program will then prompt you to enter a number.' additional_kwargs={} response_metadata={'finish_reason': 'STOP', 'model_name': 'gemini-2.5-flash', 'safety_ratings': [], 'model_provider': 'google_genai'} id='lc_run--019ede6a-6b1a-7cb0-bd9e-9feaea7d3295-0' tool_calls=[] invalid_tool_calls=[] usage_metadata={'input_tokens': 14, 'output_tokens': 2752, 'total_tokens': 2766, 'input_token_details': {'cache_read': 0}, 'output_token_details': {'reasoning': 1374}}Here's a Java program that checks whether a given number is prime or not.

The program includes:
1.  A `main` method to handle user input and display the result.
2.  A separate `isPrime` method that contains the core logic for checking primality.

```java
import java.util.Scanner;

public class PrimeChecker {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("Enter a number to check if it's prime: ");
        int number = scanner.nextInt();

        if (isPrime(number)) {
            System.out.println(number + " is a prime number.");
        } else {
            System.out.println(number + " is not a prime number.");
        }

        scanner.close(); // Close the scanner to prevent resource leaks
    }

    /**
     * Checks if a given integer is a prime number.
     * A prime number is a natural number greater than 1 that has no positive divisors
     * other than 1 and itself.
     *
     * @param number The integer to check.
     * @return true if the number is prime, false otherwise.
     */
    public static boolean isPrime(int number) {
        // 1. Handle edge cases:
        // Numbers less than or equal to 1 are not prime.
        if (number <= 1) {
            return false;
        }
        // 2 is the only even prime number.
        if (number == 2) {
            return true;
        }
        // All other even numbers are not prime.
        if (number % 2 == 0) {
            return false;
        }

        // 2. Check for divisibility by odd numbers up to the square root of the number.
        // We only need to check up to the square root because if a number 'n' has a divisor 'd'
        // greater than sqrt(n), then it must also have a divisor 'n/d' which is less than sqrt(n).
        // We start from 3 and increment by 2 (skipping even numbers) because we've already handled even numbers.
        for (int i = 3; i * i <= number; i += 2) {
            if (number % i == 0) {
                return false; // Found a divisor, so it's not prime
            }
        }

        // 3. If no divisors were found, the number is prime.
        return true;
    }
}
```

### How to Compile and Run:

1.  **Save:** Save the code as `PrimeChecker.java`.
2.  **Compile:** Open a terminal or command prompt, navigate to the directory where you saved the file, and compile it using the Java compiler:
    ```bash
    javac PrimeChecker.java
    ```
3.  **Run:** After successful compilation, run the program:
    ```bash
    java PrimeChecker
    ```

The program will then prompt you to enter a number.

### Explanation:

1.  **`main` Method:**
    *   It creates a `Scanner` object to read input from the user.
    *   It prompts the user to enter an integer.
    *   It calls the `isPrime()` method with the entered number.
    *   Based on the boolean result from `isPrime()`, it prints whether the number is prime or not.
    *   `scanner.close()` is important to release system resources.

2.  **`isPrime(int number)` Method:**
    *   **Edge Cases (`number <= 1`, `number == 2`, `number % 2 == 0`):**
        *   Numbers less than or equal to 1 are not prime by definition.
        *   `2` is the smallest and only even prime number.
        *   Any other even number (greater than 2) is divisible by 2, so it cannot be prime. These checks handle common cases quickly.
    *   **Loop for Divisors (`for (int i = 3; i * i <= number; i += 2)`):**
        *   **Starting `i = 3`:** We've already handled 2, so we start checking from the next odd number.
        *   **Condition `i * i <= number`:** This is an optimization. If a number `n` has a divisor `d`, then `n = d * k`. If `d` were greater than `sqrt(n)`, then `k` would have to be less than `sqrt(n)`. This means if `n` has any divisors, it *must* have at least one divisor less than or equal to its square root. So, we only need to check for divisors up to `sqrt(n)`. Using `i * i <= number` avoids floating-point calculations (`Math.sqrt()`) and potential precision issues, making it generally preferred for integer loops.
        *   **Increment `i += 2`:** Since we've already ruled out even numbers, we only need to check odd potential divisors. This halves the number of iterations in the loop.
        *   **`if (number % i == 0)`:** If `number` is perfectly divisible by `i` (meaning the remainder is 0), then `i` is a divisor other than 1 and itself, so the number is not prime. We immediately return `false`.
    *   **Return `true`:** If the loop completes without finding any divisors, it means the number has no divisors other than 1 and itself (and it's greater than 1), so it is prime.```java
import java.util.Scanner;

public class PrimeChecker {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("Enter a number: ");
        int num = scanner.nextInt();

        boolean isPrime = true;

        if (num <= 1) {
            isPrime = false;
        } else if (num == 2) {
            isPrime = true;
        } else if (num % 2 == 0) { // Optimization: check for even numbers greater than 2
            isPrime = false;
        } else {
            // Check for odd divisors from 3 up to the square root of num
            for (int i = 3; i * i <= num; i += 2) {
                if (num % i == 0) {
                    isPrime = false;
                    break;
                }
            }
        }

        if (isPrime) {
            System.out.println(num + " is a prime number.");
        } else {
            System.out.println(num + " is not a prime number.");
        }

        scanner.close();
    }
}
``````java
import java.util.Scanner;

public class PrimeChecker {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("Enter a number: ");
        int num = scanner.nextInt();

        if (num <= 1) {
            System.out.println(num + " is not a prime number.");
        } else if (num == 2) {
            System.out.println(num + " is a prime number.");
        } else {
            boolean isPrime = true;
            for (int i = 2; i <= Math.sqrt(num); i++) {
                if (num % i == 0) {
                    isPrime = false;
                    break;
                }
            }

            if (isPrime) {
                System.out.println(num + " is a prime number.");
            } else {
                System.out.println(num + " is not a prime number.");
            }
        }

        scanner.close();
    }
}
```